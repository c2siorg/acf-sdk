services:
  sidecar:
    build: 
      context: ./sidecar
    volumes:
      - ./config/sidecar.yaml:/config/sidecar.yaml:ro
      - ./policies:/policies:ro
    environment:
      - ACF_HMAC_KEY=${ACF_HMAC_KEY}
